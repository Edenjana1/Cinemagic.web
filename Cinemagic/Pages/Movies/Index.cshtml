@page
@model Cinemagic.Pages.Movies.IndexModel

@{
    ViewData["Title"] = "Movies";
    var userType = HttpContext.Session.GetString("UserType");
}
<!-- Genre Navigation Bar -->
<div class="genre-bar-container">
    <div class="genre-bar d-flex flex-wrap gap-2 justify-content-start">
        <a asp-page="./Index" class="btn @(Model.Genre == null ? "btn-dark" : "btn-outline-secondary")">All</a>
        @foreach (var genre in Model.Genres)
        {
            <a asp-page="./Index"
               asp-route-Genre="@genre.Value"
               class="btn @(Model.Genre?.ToString() == genre.Value ? "btn-dark" : "btn-outline-secondary")">
                @genre.Text
            </a>
        }
    </div>
</div>

<h1>Movies</h1>

@if (userType == "Admin")
{
    <a href="/Movies/Create" class="btn btn-primary">Add Movie</a>
}

<!-- Search Box -->
<form method="get" style="margin: 20px 0;">
    <div style="display: flex; flex-wrap: wrap; align-items: center; gap: 10px;">
        <label for="searchBoxMovies" style="font-weight: 500; color: #444;">Search Movies:</label>
        <input type="text" id="searchBoxMovies" name="SearchString"
               placeholder="Enter movie name..."
               style="padding: 8px 12px; border: 1px solid #ccc; border-radius: 8px; width: 220px;" />

        <input type="submit" value="Search"
               style="background-color: #444; color: white; border: none; padding: 8px 16px; border-radius: 8px;
                      transition: background-color 0.3s ease;"
               onmouseover="this.style.backgroundColor='#333';"
               onmouseout="this.style.backgroundColor='#444';" />

        <a asp-page="./Index"
           style="text-decoration: none; padding: 8px 12px; border-radius: 8px; border: 1px solid #aaa; color: #555;
                  transition: background-color 0.3s ease;"
           onmouseover="this.style.backgroundColor='#f0f0f0';"
           onmouseout="this.style.backgroundColor='transparent';">
            Back to Full List
        </a>
    </div>
</form>


<h3>Movies</h3>
@if (Model.Movie.Any())
{
    <div class="movie-grid">
        @foreach (var item in Model.Movie)
        {
            <div style="position: relative; width: 100%; overflow: hidden; margin-bottom: 40px;">

                <button type="button" id="scrollLeftKids" style="position: absolute; left: 0; top: 40%; z-index: 10; background-color: rgba(0,0,0,0.3); border: none; color: white; font-size: 24px; cursor: pointer; padding: 10px;">&#8592;</button>
                <button type="button" id="scrollRightKids" style="position: absolute; right: 0; top: 40%; z-index: 10; background-color: rgba(0,0,0,0.3); border: none; color: white; font-size: 24px; cursor: pointer; padding: 10px;">&#8594;</button>
                <a asp-page="./Details" asp-route-id="@item.MovieID">
                    <img src="~/Images/MovieIMG/@item.Image" alt="@item.MovieName" />
                    <h5>@item.MovieName</h5>
                </a>
            </div>
        }
    </div>
}
else
{
    <p style="font-style: italic; color: #aaa;">No movies available.</p>
}

<h3>Most Purchased Movies</h3>
@if (Model.MostPurchasedMovies.Any())
{
    <div style="position: relative; width: 100%; overflow: hidden; margin-bottom: 40px;">

        <button type="button" id="scrollLeftKids" style="position: absolute; left: 0; top: 40%; z-index: 10; background-color: rgba(0,0,0,0.3); border: none; color: white; font-size: 24px; cursor: pointer; padding: 10px;">&#8592;</button>
        <button type="button" id="scrollRightKids" style="position: absolute; right: 0; top: 40%; z-index: 10; background-color: rgba(0,0,0,0.3); border: none; color: white; font-size: 24px; cursor: pointer; padding: 10px;">&#8594;</button>
        @foreach (var movie in Model.MostPurchasedMovies)
        {
            <div class="movie-card">
                <a asp-page="./Details" asp-route-id="@movie.MovieID">
                    <img src="~/Images/MovieIMG/@movie.Image" alt="@movie.MovieName" />
                    <h5>@movie.MovieName</h5>
                </a>
            </div>
        }
    </div>
}
else
{
    <p style="font-style: italic; color: #aaa;">No popular movies available.</p>
}

<h3>New Movies</h3>
@if (Model.RecentMovies.Any())
{
    <div style="position: relative; width: 100%; overflow: hidden; margin-bottom: 40px;">

        <button type="button" id="scrollLeftKids" style="position: absolute; left: 0; top: 40%; z-index: 10; background-color: rgba(0,0,0,0.3); border: none; color: white; font-size: 24px; cursor: pointer; padding: 10px;">&#8592;</button>
        <button type="button" id="scrollRightKids" style="position: absolute; right: 0; top: 40%; z-index: 10; background-color: rgba(0,0,0,0.3); border: none; color: white; font-size: 24px; cursor: pointer; padding: 10px;">&#8594;</button>
        @foreach (var movie in Model.RecentMovies)
        {
            <div class="movie-card">
                <a asp-page="./Details" asp-route-id="@movie.MovieID">
                    <img src="~/Images/MovieIMG/@movie.Image" alt="@movie.MovieName" />
                    <h5>@movie.MovieName</h5>
                </a>
            </div>
        }
    </div>
}
else
{
    <p style="font-style: italic; color: #aaa;">No new movies available.</p>
}

<h3>Movies for Kids</h3>
@if (Model.KidsMovies.Any())
{
    <div style="position: relative; width: 100%; overflow: hidden; margin-bottom: 40px;">

        <button type="button" id="scrollLeftKids" style="position: absolute; left: 0; top: 40%; z-index: 10; background-color: rgba(0,0,0,0.3); border: none; color: white; font-size: 24px; cursor: pointer; padding: 10px;">&#8592;</button>
        <button type="button" id="scrollRightKids" style="position: absolute; right: 0; top: 40%; z-index: 10; background-color: rgba(0,0,0,0.3); border: none; color: white; font-size: 24px; cursor: pointer; padding: 10px;">&#8594;</button>
        @foreach (var movie in Model.KidsMovies)
        {
            <div class="movie-card">
                <a asp-page="./Details" asp-route-id="@movie.MovieID">
                    <img src="~/Images/MovieIMG/@movie.Image" alt="@movie.MovieName" />
                    <h5>@movie.MovieName</h5>
                </a>
            </div>
        }
    </div>
}
else
{
    <p style="font-style: italic; color: #aaa;">No kids movies available.</p>
}

